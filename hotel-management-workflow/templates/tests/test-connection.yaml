apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hotel-management-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "hotel-management-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hotel-management-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
